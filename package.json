{
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "pnpm --filter '*' lint",
    "lint:fix": "pnpm --filter '*' lint:fix",
    "test": "pnpm --filter '*' test"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">= 20.*",
    "yarn": "use pnpm",
    "npm": "use pnpm"
  },
  "volta": {
    "node": "20.9.0",
    "pnpm": "8.14.0"
  }
}
